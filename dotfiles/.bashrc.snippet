# --- sandbox-setup managed ---
# mise (node, etc.) on PATH for both interactive + non-interactive shells
export PATH="$HOME/.local/bin:$PATH"
if command -v mise >/dev/null 2>&1; then
  eval "$(mise activate bash)"
fi
